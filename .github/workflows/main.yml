name: Deploy React App to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: skill-swap-platform/frontend/package-lock.json

      - name: Install dependencies
        working-directory: skill-swap-platform/frontend
        run: npm ci

      - name: Build
        working-directory: skill-swap-platform/frontend
        run: npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: skill-swap-platform/frontend/dist

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
